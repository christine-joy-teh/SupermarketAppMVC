<<<<<<< HEAD
<%- include('partials/head', { pageTitle: 'NETS QR Error - Supermarket App' }) %>
<%- include('partials/navbar', {
  navLinksRight: [
    { href: '/cart', label: 'Cart', badgeValue: cartCount, badgeClass: 'bg-primary', classes: 'position-relative' },
    { href: '/orders/history', label: 'My Orders' },
    { href: '/shopping', label: 'Shop' },
    { href: '/logout', label: 'Logout' }
  ]
}) %>
  <div class="container mt-4">
    <div class="alert alert-danger">
      <h5 class="mb-1"><%= title || 'Error' %></h5>
      <div class="small">Response code: <%= responseCode || 'N.A.' %></div>
      <% if (instructions) { %>
        <div class="small mt-1"><%= instructions %></div>
      <% } %>
      <div class="mt-2"><%= errorMsg || 'Unable to generate NETS QR code.' %></div>
    </div>
    <a href="/payment" class="btn btn-secondary">Back to Payment</a>
  </div>
=======
<%- include('partials/head', { pageTitle: 'NETS QR Failed | Supermarket', bodyClass: 'bg-surface' }) %>
<%- include('partials/navbar') %>

<main class="page-section">
  <div class="container">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <h4 class="mb-2"><%= title %></h4>
        <p class="text-muted mb-3"><%= errorMsg %></p>

        <div class="alert alert-warning">
          <div>Response code: <%= responseCode %></div>
          <% if (instructions) { %>
            <div>Instructions: <%= instructions %></div>
          <% } %>
        </div>

        <div class="d-flex gap-2">
          <a class="btn btn-secondary fw-semibold" href="/payment">Back to payment</a>
          <a class="btn btn-success fw-semibold" href="/shopping">Continue shopping</a>
        </div>
      </div>
    </div>
  </div>
</main>

>>>>>>> bfc95a4 (new updates, transaction logs and refund)
<%- include('partials/footer') %>
